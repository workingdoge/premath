# Interop Core Fixtures

Executable vectors for Interop Core slices:

- `draft/KCIR-CORE`
- `draft/REF-BINDING`
- `draft/NF`
- `draft/WIRE-FORMATS`
- `draft/ERROR-CODES`

Retained for native checker and Rust adapter coverage. The old Python suite
wrapper is retired. Run with:

```bash
cargo test --workspace
```
