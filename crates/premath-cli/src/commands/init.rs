pub fn run(path: String) {
    println!("premath init {path}");
    println!();
    println!("  Creates local layout for adapter composition:");
    println!("    - `.beads/issues.jsonl` (memory source)");
    println!("    - optional JJ workspace metadata");
    println!("    - optional Surreal query cache directory");
}
