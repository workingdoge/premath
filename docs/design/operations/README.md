# Operations Design

Implementation-facing operator playbooks. These docs are non-normative and
must defer to `../../../specs/` for contract authority.

Premath no longer hosts tracker execution-loop playbooks. Worker-loop,
claim/lease, and prioritization policy belong to Tusk/downstream tracker
surfaces; Premath only checks normalized boundary claims.
